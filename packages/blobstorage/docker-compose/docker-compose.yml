version: "3.8"
services:
  db:
    image: mysql:8.0
    cap_add:
      - SYS_NICE
    restart: always
    environment:
      - MYSQL_DATABASE=root
      - MYSQL_ROOT_PASSWORD=passw00d
    ports:
      - "17000:24"
    volumes:
      - ~/.docker-conf/mysql/data/:/var/lib/mysql
